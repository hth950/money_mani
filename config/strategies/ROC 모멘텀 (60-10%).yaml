name: ROC 모멘텀 (60/10%)
description: ROC(60) 10% 이상 상승 모멘텀시 매수, 0% 이하 청산
source: auto_generated
category: momentum
status: archived
rules:
  entry:
  - condition: threshold
    indicator: ROC_60
    direction: above
    value: 10
  exit:
  - condition: threshold
    indicator: ROC_60
    direction: below
    value: 0
indicators:
- type: roc
  column: close
  output_name: ROC_60
  period: 60
parameters:
  position_size: 1.0
  stop_loss: 0.05
timeframe: 1d
backtest_results: {}
